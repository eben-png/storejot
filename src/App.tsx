import "./App.css";
import IdeaInput from "./components/UI/IdeaInput";

const App: React.FC = () => {
  return (
    <main className="w-full h-screen flex items-center justify-center font-mono">
      <IdeaInput />
    </main>
  )
}

export default App;
